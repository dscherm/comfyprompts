# Bridge Pipeline Resume

<!-- Auto-injected by inject_lessons_interactive.py at SessionStart. -->
<!-- Bridge state survives sessions; read this BEFORE acting on tasks. -->

**Pipeline:** `51771284` | **Phase:** `intake` | **Age:** 18.6 days (abandoned)
**Tasks:** 0 done, 0 active, 0 pending

## ⚠️ PIPELINE ABANDONED

No activity for 19 days and zero observations recorded. Ask the user whether to resume this pipeline or discard it before continuing any task.

## Next Step

Run a deep-interview with the user (OMC's `deep-interview` skill).
Write the crystallized spec to `.ralph/spec.md`, populate `plan.md` with JSON task blocks (each with an `id`), then:

```bash
python $RALPH_HOME/tools/bridge_state.py refresh
python $RALPH_HOME/tools/bridge_state.py phase plan
```

Re-derive any time with:
```bash
python $RALPH_HOME/tools/bridge_state.py what-now
```

