# Gate 4 — TRAIN-B · GPU-GATED

- [ ] **Human GPU-free confirmation obtained BEFORE training** (recorded).
- [ ] Kontext edit LoRA trained on the aligned `(ink, clay)` pairs with the fixed
      instruction; real checkpoints in `E:/ai-training/flux-output/ink_to_clay_v1_b/`.
- [ ] The Kontext training path was verified (not assumed) — if launch_train.py
      lacked Kontext support, the explicit config used is committed.
- [ ] ComfyUI restarted and generation works.

Fail → fix the Kontext config/data format and retrain to a fresh output name.
