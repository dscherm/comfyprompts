{"task": 1, "category": "critical", "description": "Parse PRD and generate intake report with asset list, style profile, and kart hierarchy spec", "steps": ["Read prd.md", "Extract 10 kart assets with body types and style variants", "Load MK reference kart proportions (977 verts, transform hierarchy, no armature)", "Determine modular split regions from kart_hierarchy.md", "Write intake-report.json with MK-compatible hierarchy target", "Validate all required fields present"], "passes": true}
{"task": 2, "category": "critical", "description": "Generate 2D concept art for current kart with style-consistent prompts", "steps": ["Load intake report", "Craft style-encoded prompt per kart's assigned variant (v2/v4/v5/v7/v9)", "Generate front 3/4 view via ComfyUI Flux/SDXL", "Apply negative prompt (no wheels, no driver, no roof)", "Save with naming convention {kart_id}_v{N}_front.png"], "passes": true}
{"task": 3, "category": "critical", "description": "Remove background on all generated concept art", "steps": ["Run remove_background ComfyUI workflow on each image", "Validate transparent PNG output", "Save cleaned images to output/cleaned/"], "passes": true}
{"task": 4, "category": "critical", "description": "Convert cleaned 2D artwork to 3D meshes via ComfyUI with back-pressure", "steps": ["For each cleaned image, run Hunyuan3D v2.0 via ComfyUI MCP", "Back-pressure: max 2 concurrent ComfyUI jobs (semaphore)", "On VRAM OOM: halve octree_resolution (256->128->64->32)", "On model failure: fallback chain Hunyuan3D v2.0 -> Turbo -> TripoSG -> TripoSR", "On timeout: retry up to 3 times with backoff", "Save checkpoint after each successful generation", "Save raw GLB to output/meshes/"], "passes": true}
{"task": 5, "category": "critical", "description": "Mesh prep + modular split via Blender (headless or blender-mcp)", "steps": ["Run mesh_prep.py: fill holes, remove doubles, recalc normals, decimate if >20k faces", "Run mesh_split.py: split single mesh into 7 regions (Chassis, Hood, Bumper_Front/Rear, Panel_L/R, Spoiler)", "All 10 karts prepared and split successfully"], "passes": true}
{"task": 6, "category": "critical", "description": "Assemble modular hierarchy + export to all target formats", "steps": ["Run kart_assembler.py: build MK-style parent-child hierarchy", "Create 16 empties (wheel mounts, axles, exhaust, seat, FX points)", "Parent detachable meshes to Chassis, axles to KartRoot", "Export FBX for Unity (-Z forward, Y up)", "Export GLB for Blender", "All 10 karts assembled and exported"], "passes": true}
{"task": 7, "category": "high", "description": "Batch process all 10 karts end-to-end", "steps": ["batch_kart_pipeline.py processed all 10 karts: split -> assemble -> export", "10/10 succeeded in 53.5s", "BATCH-MANIFEST.json written"], "passes": true}
{"task": 8, "category": "high", "description": "Final validation and manifest", "steps": ["Verified all 10 karts have: concept PNG, cleaned PNG, split GLB, assembled FBX+GLB", "BATCH-MANIFEST.md written with full inventory", "Pipeline state updated: all 8 stages complete"], "passes": true}
