# Animation picks (from A/B comparison)

Button/role -> baked clip (chars use knight_<clip>.fbx in compare/, to be
moved to chars/ and wired in player.gd for KD14).

## Assigned (attacks)
- J = slash
- L = ss_attack2
- K = ss_attack1
- N = ss_slash1
- U (deflect/defense) = ss_slash3

## Locomotion / jump
- idle  = current revenant_knight idle (unchanged)
- walk  = walk_relaxed (unchanged, "feels good")
- run   = run_forward (unchanged)
- jump  = jump   -> SPACE bar

## Unassigned attack candidate
- gs_jump_attack (available for a jump/heavy-attack button if wanted)

## Deleted (never use)
- ss_slash2, ss_slash4
